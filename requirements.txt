# ============================================================================
# ComfyUI Discord Bot - Python Dependencies
# ============================================================================
# Install all dependencies with: pip install -r requirements.txt
# Or with uv (faster): uv pip install -r requirements.txt
# ============================================================================

# ----------------------------------------------------------------------------
# DISCORD BOT
# ----------------------------------------------------------------------------

# Discord API library (py-cord - supports slash commands)
py-cord==2.6.1

# Alternative if you prefer discord.py with slash commands:
# discord.py==2.3.2

# ----------------------------------------------------------------------------
# ASYNC HTTP REQUESTS
# ----------------------------------------------------------------------------

# Async HTTP client for API calls
aiohttp==3.9.5

# HTTP requests (sync backup)
requests==2.31.0

# ----------------------------------------------------------------------------
# MODAL CLI & API
# ----------------------------------------------------------------------------

# Modal Python client (for CLI commands and API)
modal==0.63.64

# ----------------------------------------------------------------------------
# ENCRYPTION & SECURITY
# ----------------------------------------------------------------------------

# Encryption for storing Modal tokens
cryptography==42.0.5

# ----------------------------------------------------------------------------
# DATABASE
# ----------------------------------------------------------------------------

# SQLite is built into Python, but we need this for better support
# (Actually, sqlite3 is built-in, so this is optional)
# If you want advanced features, uncomment:
# sqlalchemy==2.0.29

# ----------------------------------------------------------------------------
# ENVIRONMENT VARIABLES
# ----------------------------------------------------------------------------

# Load environment variables from .env file
python-dotenv==1.0.1

# ----------------------------------------------------------------------------
# UTILITIES
# ----------------------------------------------------------------------------

# JSON handling (built into Python, but this is for advanced features)
# Not needed, using built-in json

# Path handling (built into Python via pathlib)
# Not needed

# Async utilities (built into Python via asyncio)
# Not needed

# ----------------------------------------------------------------------------
# LOGGING
# ----------------------------------------------------------------------------

# Logging is built into Python
# But if you want colored logs (optional):
# coloredlogs==15.0.1

# ----------------------------------------------------------------------------
# OPTIONAL: DEVELOPMENT TOOLS
# ----------------------------------------------------------------------------

# Code formatting (recommended)
# black==24.3.0

# Linting (recommended)
# flake8==7.0.0
# pylint==3.1.0

# Type checking (recommended)
# mypy==1.9.0

# Testing (if you write tests)
# pytest==8.1.1
# pytest-asyncio==0.23.6

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
#
# INSTALLATION INSTRUCTIONS:
#
# 1. Create a virtual environment (recommended):
#    python -m venv venv
#    source venv/bin/activate  # On Linux/Mac
#    venv\Scripts\activate     # On Windows
#
# 2. Install dependencies:
#    pip install -r requirements.txt
#
# 3. Or use uv for faster installation:
#    pip install uv
#    uv pip install -r requirements.txt
#
# UPGRADING DEPENDENCIES:
#    pip install --upgrade -r requirements.txt
#
# CHECKING WHAT'S INSTALLED:
#    pip list
#    pip show package-name
#
# TROUBLESHOOTING:
# - If installation fails, try upgrading pip first:
#   pip install --upgrade pip
#
# - On some systems, you might need to use python3 and pip3:
#   python3 -m pip install -r requirements.txt
#
# - If you get SSL errors, try:
#   pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org -r requirements.txt
#
# ============================================================================

# ----------------------------------------------------------------------------
# MINIMUM PYTHON VERSION
# ----------------------------------------------------------------------------
# This bot requires Python 3.10 or higher
# Check your version with: python --version
#
# Discord bots work best with Python 3.10+
# Modal requires Python 3.10+
# ============================================================================
